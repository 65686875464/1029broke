# MyApp

Project generated from Vladimir Andreyev template.

Author: Kim

Display name: MyApp

Run locally (developer)

Requirements:
- Node.js >= 18
- npm or yarn
- expo (optional)

Commands:

```powershell
# install deps (if package.json provided)
npm install

# start Metro / Expo
npx expo start
```

If your platform provides a web container/preview you can skip local setup — just select the template and run in the platform UI.

Note for users: this template is designed to be run by your platform (web container/preview). You do NOT need to install Docker or Node locally to use the template unless you want to develop it on your machine.

